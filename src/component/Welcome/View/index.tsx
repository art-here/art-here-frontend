import styled from "@emotion/styled";
import HeroSection from "../HeroSection";

const WelcomeView = () => {
  {
    /* TODO: 
       - 일단 뷰 컴포넌트 추가
       - 이후 웰컴 컴포넌트로 수정
       -  유저 스크롤 or 몇 초 뒤 자동 사라짐  
       {isFirstVisit ? <WelcomeView /> : <></>}

  const [isFirstVisit, setIsFirstVisit] = useState(true);
       */
  }
  return (
    <>
      <HeroSection />
    </>
  );
};

export default WelcomeView;
